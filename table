Question:
Create the multiplication table of a given number N and return the table as an array.

Solution:
class Solution
{
public:
    vector<int> getTable(int N)
    {
        vector<int> a;
        for(int i=1;i<=10;i++)
        {
            a.push_back(N*i);
        }
        return a;
    }
    
};

Explaination:
Declare a vector of type int to return the table as output.
using for loop multiply the value and push the value into the array.
return the array.
